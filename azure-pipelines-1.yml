# Maven
# Build your Java project and run tests with Apache Maven.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java

trigger:
- main

pool:
  name: DevOps  # Your self-hosted agent pool

steps:
- task: Maven@3
  displayName: 'Maven Package'
  inputs:
    mavenPomFile: 'happytails/pom.xml'
    mavenOptions: '-Xmx3072m'
    javaHomeOption: 'Path'  # Changed from JDKVersion to Path
    jdkDirectory: 'C:\Program Files\Eclipse Adoptium\jdk-17.0.14.7-hotspot'  # Direct path to JDK
    jdkArchitectureOption: 'x64'
    publishJUnitResults: true
    testResultsFiles: '**/surefire-reports/TEST-*.xml'
    goals: 'package -DskipTests'